export enum ActivityType {
  PROBLEM = 'problem',
  CONCEPT = 'concept',
  REVISION = 'revision',
  OTHER = 'other',
}

export enum ActivityStatus {
  COMPLETED = 'completed',
  IN_PROGRESS = 'in-progress',
}
